@model IEnumerable<wardrobe.Models.FootwearType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FootwearTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FootwearTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FootwearTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FootwearTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FootwearTypeID })
        </td>
    </tr>
}

</table>
